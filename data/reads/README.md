# Placeholder location for input reads

- Specify read paths in the `samplesheet.csv`
- Paths can be absolute, or relative to the base directory (`projectDir`), e.g.: `./data/reads/file.fq.gz`, Nextflow can resolve these
