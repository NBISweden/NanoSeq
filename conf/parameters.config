/* 
----------------------------------------------------------------------------------------
Default parameter configuration
----------------------------------------------------------------------------------------
*/

// General params

	params.help = null
	params.samplesheetPath = "${projectDir}/data"
	params.samplesheet = "samplesheet.csv"

// Reference file params: unless set, defaults to value right of the Elvis operator

	params.referencePath = "${projectDir}/data/genome/"
	params.fasta = "genome.fa"








/*
----------------------------------------------------------------------------------------

// TEMP STORE OF ORIGINAL PARAMS

	// params.outDir = baseDir

// preprocessing and mapping params
	//params.fastq_tags_m = 3
	//params.fastq_tags_s = 4
	//params.nanoseq_dedup_m = 1 # bug found by ao7. It was not used later though.

// *** NanoSeq parameters
params.jobs = 100

----------------------------------------------------------------------------------------
*/